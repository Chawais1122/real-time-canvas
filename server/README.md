# Real-time Canvas Server

Socket.io server powering the collaborative canvas.

## Scripts
- `npm run dev` — start in watch mode
- `npm run build && npm start` — build and run

## Env
See `.env.example` for defaults.
